<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Menú digital de La Cantinita: bebidas y contacto por WhatsApp.">
    <title>La Cantinita - Menú Digital</title>
    <style>
        :root {
            --primario: #d4a017; /* Color dorado */
            --oscuro: #1a1a1a;
            --blanco: #ffffff;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: var(--oscuro);
            color: var(--blanco);
            margin: 0;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            color: var(--primario);
            font-size: 2.5rem;
            margin-bottom: 30px;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        main {
            width: 100%;
            max-width: 640px;
        }

        /* Contenedor de las tarjetas */
        .menu-container {
            width: 100%;
            max-width: 500px;
            display: flex;
            flex-direction: column;
            gap: 20px;
            margin: 0 auto;
        }

        /* Tarjeta con Animación de entrada */
        .card {
            background: #2a2a2a;
            border-radius: 15px;
            overflow: hidden;
            cursor: pointer;
            position: relative;
            perspective: 1000px;
            min-height: 200px;
            opacity: 0;
            transform: translateY(20px);
            animation: fadeIn 0.6s forwards;
            animation-delay: var(--delay, 0s);
        }

        @keyframes fadeIn {
            to { opacity: 1; transform: translateY(0); }
        }

        .card-inner {
            position: relative;
            width: 100%;
            height: 100%;
            text-align: center;
            transition: transform 0.6s;
            -webkit-transform-style: preserve-3d;
            transform-style: preserve-3d;
        }

        /* Clase que se activa al hacer click */
        .card.flipped .card-inner {
            transform: rotateY(180deg);
        }

        .front, .back {
            position: absolute;
            width: 100%;
            height: 100%;
            -webkit-backface-visibility: hidden;
            backface-visibility: hidden;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 20px;
            box-sizing: border-box;
        }

        .front img {
            width: 80px;
            height: 80px;
            object-fit: contain;
            margin-bottom: 10px;
        }

        .back {
            background: var(--primario);
            color: var(--oscuro);
            transform: rotateY(180deg);
        }

        /* Focus visible para accesibilidad */
        .card:focus-visible {
            outline: 3px solid #ffd880;
            outline-offset: 4px;
        }

        /* Sección de Contacto */
        .whatsapp-section {
            margin-top: 40px;
            text-align: center;
            opacity: 0;
            animation: fadeIn 1s 1s forwards; /* Aparece después */
        }

        .btn-whatsapp {
            background-color: #25d366;
            color: white;
            padding: 15px 30px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: bold;
            display: inline-block;
            margin-top: 10px;
            transition: transform 0.3s;
        }

        .btn-whatsapp:hover, .btn-whatsapp:focus-visible {
            transform: scale(1.05);
            outline: none;
        }

        /* Reduce motion for users who prefer it */
        @media (prefers-reduced-motion: reduce) {
            .card, .card-inner, .btn-whatsapp, .whatsapp-section {
                animation: none !important;
                transition: none !important;
            }
        }
    </style>
</head>
<body>

    <h1>La Cantinita</h1>

    <main>
        <div class="menu-container">
            <div class="card" role="button" tabindex="0" aria-pressed="false" data-card style="--delay:0s;">
                <div class="card-inner" aria-hidden="false">
                    <div class="front">
                        <img src="https://cdn-icons-png.flaticon.com/512/931/931949.png" alt="Copa de Margarita" loading="lazy">
                        <h2>Margarita Clásica</h2>
                    </div>
                    <div class="back">
                        <h3>Especificaciones</h3>
                        <p>Contenido: Tequila, Triple Sec, Jugo de Limón.</p>
                        <p><strong>Alcohol: 3.5 oz</strong></p>
                    </div>
                </div>
            </div>

            <div class="card" role="button" tabindex="0" aria-pressed="false" data-card style="--delay:0.2s;">
                <div class="card-inner" aria-hidden="false">
                    <div class="front">
                        <img src="https://cdn-icons-png.flaticon.com/512/3100/3100614.png" alt="Vaso de Whisky" loading="lazy">
                        <h2>Old Fashioned</h2>
                    </div>
                    <div class="back">
                        <h3>Especificaciones</h3>
                        <p>Contenido: Bourbon, Angostura, Azúcar.</p>
                        <p><strong>Alcohol: 2.0 oz</strong></p>
                    </div>
                </div>
            </div>
        </div>

        <div class="whatsapp-section">
            <p>¿Listo para ordenar?</p>
            <a href="https://wa.me/1234567890?text=Hola!%20Quiero%20ordenar%20de%20La%20Cantinita" class="btn-whatsapp" aria-label="Enviar mensaje por WhatsApp">
                Enviar Mensaje al WhatsApp
            </a>
        </div>
    </main>

    <script>
        // Manejo de interacciones sin JS inline. Soporta click y teclado (Enter/Espacio).
        (function() {
            const cards = document.querySelectorAll('[data-card]');
            cards.forEach(card => {
                const inner = card.querySelector('.card-inner');
                function setState(flipped) {
                    card.classList.toggle('flipped', flipped);
                    card.setAttribute('aria-pressed', String(flipped));
                    inner.setAttribute('aria-hidden', String(!flipped));
                }
                function toggle() {
                    const flipped = card.classList.toggle('flipped');
                    card.setAttribute('aria-pressed', String(flipped));
                    inner.setAttribute('aria-hidden', String(!flipped));
                }
                card.addEventListener('click', () => toggle());
                card.addEventListener('keydown', e => {
                    if (e.key === 'Enter' || e.key === ' ') {
                        e.preventDefault();
                        toggle();
                    }
                });
                // Initialize aria-hidden based on class state
                const isFlipped = card.classList.contains('flipped');
                setState(isFlipped);
            });
        })();
    </script>

</body>
</html>