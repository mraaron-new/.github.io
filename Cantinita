<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>La Cantinita - Menú Digital</title>
    <style>
        :root {
            --primario: #d4a017; /* Color dorado/alcohol */
            --oscuro: #1a1a1a;
            --blanco: #ffffff;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: var(--oscuro);
            color: var(--blanco);
            margin: 0;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            color: var(--primario);
            font-size: 2.5rem;
            margin-bottom: 30px;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        /* Contenedor de las tarjetas */
        .menu-container {
            width: 100%;
            max-width: 500px;
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        /* Tarjeta con Animación de entrada */
        .card {
            background: #2a2a2a;
            border-radius: 15px;
            overflow: hidden;
            cursor: pointer;
            position: relative;
            perspective: 1000px;
            height: 200px;
            opacity: 0;
            transform: translateY(20px);
            animation: fadeIn 0.6s forwards;
        }

        @keyframes fadeIn {
            to { opacity: 1; transform: translateY(0); }
        }

        .card-inner {
            position: relative;
            width: 100%;
            height: 100%;
            text-align: center;
            transition: transform 0.6s;
            transform-style: preserve-3d;
        }

        /* Clase que se activa al hacer click */
        .card.flipped .card-inner {
            transform: rotateY(180deg);
        }

        .front, .back {
            position: absolute;
            width: 100%;
            height: 100%;
            backface-visibility: hidden;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 20px;
            box-sizing: border-box;
        }

        .front img {
            width: 80px;
            height: 80px;
            object-fit: contain;
            margin-bottom: 10px;
        }

        .back {
            background: var(--primario);
            color: var(--oscuro);
            transform: rotateY(180deg);
        }

        /* Sección de Contacto */
        .whatsapp-section {
            margin-top: 40px;
            text-align: center;
            opacity: 0;
            animation: fadeIn 1s 1s forwards; /* Aparece después */
        }

        .btn-whatsapp {
            background-color: #25d366;
            color: white;
            padding: 15px 30px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: bold;
            display: inline-block;
            margin-top: 10px;
            transition: transform 0.3s;
        }

        .btn-whatsapp:hover {
            transform: scale(1.1);
        }
    </style>
</head>
<body>

    <h1>La Cantinita</h1>

    <div class="menu-container">
        <div class="card" onclick="this.classList.toggle('flipped')">
            <div class="card-inner">
                <div class="front">
                    <img src="https://cdn-icons-png.flaticon.com/512/931/931949.png" alt="Margarita">
                    <h2>Margarita Clásica</h2>
                </div>
                <div class="back">
                    <h3>Especificaciones</h3>
                    <p>Contenido: Tequila, Triple Sec, Jugo de Limón.</p>
                    <p><strong>Alcohol: 3.5 oz</strong></p>
                </div>
            </div>
        </div>

        <div class="card" style="animation-delay: 0.2s;" onclick="this.classList.toggle('flipped')">
            <div class="card-inner">
                <div class="front">
                    <img src="https://cdn-icons-png.flaticon.com/512/3100/3100614.png" alt="Whisky">
                    <h2>Old Fashioned</h2>
                </div>
                <div class="back">
                    <h3>Especificaciones</h3>
                    <p>Contenido: Bourbon, Angostura, Azúcar.</p>
                    <p><strong>Alcohol: 2.0 oz</strong></p>
                </div>
            </div>
        </div>
    </div>

    <div class="whatsapp-section">
        <p>¿Listo para ordenar?</p>
        <a href="https://wa.me/1234567890?text=Hola!%20Quiero%20ordenar%20de%20La%20Cantinita" class="btn-whatsapp">
            Enviar Mensaje al WhatsApp
        </a>
    </div>

</body>
</html>
